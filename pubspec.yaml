name: classhub
description: A production-ready School Management Flutter app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Database
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # State Management
  provider: ^6.1.1
  
  # UI & Navigation
  cupertino_icons: ^1.0.6
  intl: ^0.18.1
  
  # File Operations
  path_provider: ^2.1.1
  
  # Export/Import
  share_plus: ^7.2.1
  file_picker: ^10.3.6
  shared_preferences: ^2.5.3
  
  # Image Picker
  
  # Permissions
  
  # Accessibility

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/

